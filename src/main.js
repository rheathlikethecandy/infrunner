let config = {
    type: Phaser.CANVAS,
    width: 640,
    height: 480,
    scene: [****, ****]
}
let game = new Phaser.Game(config);
//reserve keyboard vars
let keyUP, keyLEFT, keyRIGHT, keyDOWN, keyA, keyD, keyW, keyS;